package com.example.ebook_reader.InterfacePackage.BookShelf

import com.example.ebook_reader.Enum.BookType
import com.example.ebook_reader.entities.BookView

interface BooksAdapterOpenActivity {
    fun openActivity(book: BookView)
}